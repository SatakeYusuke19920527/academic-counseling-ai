export const getOpenAIData = async (message: string): Promise<any[]> => {
  return new Promise(async (resolve, reject) => {
    console.log('🚀 ~ returnnewPromise ~ 推論開始 ~ 🚀');
    try {
      // AI担当の人へ Do your Best!
    } catch (error: any) {
      console.error(`リクエストに失敗しました。エラー: ${error.message}`);
    }
  });
};
